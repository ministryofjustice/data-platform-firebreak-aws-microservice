{
    "Effect": "Allow",
    "Principal": {
        "Federated": "{{ oidc_arn }}/"
    },
    "Action": "sts:AssumeRoleWithWebIdentity",
    "Condition": {
        "StringEquals": {
            "{{ oidc_domain }}/:sub": "{{ user_oidc_id }}"
        }
    }
}
